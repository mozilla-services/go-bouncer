[flake8]
ignore = E501

[isort]
default_section = THIRDPARTY
known_first_party = tests

[tool:pytest]
addopts = --verbose -r=a
testpaths = tests
xfail_strict = true
base_url = http://bouncer-bouncer.stage.mozaws.net
